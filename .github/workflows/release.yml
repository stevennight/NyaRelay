name: Release

on:
  push:
    tags:
      - v*

permissions: {}

jobs:
  release-assets:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      build_date: ${{ steps.release-meta.outputs.build_date }}

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Prepare release metadata
        id: release-meta
        run: echo "build_date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Build node binaries
        env:
          NYARELAY_UPDATE_PUBLIC_KEY: ${{ secrets.NYARELAY_UPDATE_PUBLIC_KEY }}
        run: |
          mkdir -p dist
          update_public_key="$(printf '%s' "$NYARELAY_UPDATE_PUBLIC_KEY" | tr -d '[:space:]')"
          LDFLAGS="-X nyarelay/internal/shared/version.Version=${GITHUB_REF_NAME} -X nyarelay/internal/shared/version.Commit=${GITHUB_SHA} -X nyarelay/internal/shared/version.BuildDate=${{ steps.release-meta.outputs.build_date }} -X nyarelay/internal/shared/version.UpdatePublicKey=${update_public_key}"
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -buildvcs=false -ldflags "$LDFLAGS" -o dist/nyarelay-node-linux-amd64 ./cmd/node
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -buildvcs=false -ldflags "$LDFLAGS" -o dist/nyarelay-node-linux-arm64 ./cmd/node
          sha256sum dist/* > dist/SHA256SUMS

      - name: Sign node release manifest
        id: node-release
        env:
          NYARELAY_UPDATE_SIGNING_KEY: ${{ secrets.NYARELAY_UPDATE_SIGNING_KEY }}
          NYARELAY_UPDATE_PUBLIC_KEY: ${{ secrets.NYARELAY_UPDATE_PUBLIC_KEY }}
        run: |
          signing_key_file="$RUNNER_TEMP/nyarelay-update-signing.key"
          trap 'rm -f "$signing_key_file"' EXIT
          printf '%s' "$NYARELAY_UPDATE_SIGNING_KEY" > "$signing_key_file"
          update_public_key="$(printf '%s' "$NYARELAY_UPDATE_PUBLIC_KEY" | tr -d '[:space:]')"
          go run ./cmd/release-manifest \
            --node-dir dist \
            --version "${GITHUB_REF_NAME}" \
            --commit "${GITHUB_SHA}" \
            --build-date "${{ steps.release-meta.outputs.build_date }}" \
            --private-key-file "$signing_key_file" \
            --expected-public-key "$update_public_key" \
            --manifest dist/node-release-manifest.json \
            --signature dist/node-release-manifest.sig \
            --public-key dist/node-release-public.key \
            --github-output "$GITHUB_OUTPUT"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          files: dist/*

  docker-image:
    runs-on: ubuntu-latest
    needs: release-assets
    permissions:
      contents: read
      packages: write

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Prepare image name
        run: echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/nyarelay" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Log in to GHCR
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=tag
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            NYARELAY_VERSION=${{ github.ref_name }}
            NYARELAY_COMMIT=${{ github.sha }}
            NYARELAY_BUILD_DATE=${{ needs.release-assets.outputs.build_date }}
          secrets: |
            nyarelay_update_signing_key=${{ secrets.NYARELAY_UPDATE_SIGNING_KEY }}
            nyarelay_update_public_key=${{ secrets.NYARELAY_UPDATE_PUBLIC_KEY }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
